Backend (Node + Express) — Setup

1. Install dependencies
   cd backend
   npm install

2. Start
   npm start

Backend will serve a single endpoint:
GET /api/sales  (see docs for query params)

By default the backend uses the file data/sales.json for demo data.
Replace it with the real dataset (converted to JSON) or wire a MongoDB collection.
